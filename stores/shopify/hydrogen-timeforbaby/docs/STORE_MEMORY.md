# Store memory — Sol's persistent knowledge of THIS store (read once, update after changes)

Sol: this is your memory of the store. It's injected into your prompt so you DON'T re-read
everything each time. After you change something, APPEND a short line under "Recent changes".

## Map (where things are)
- **`app/theme.config.json`** — drives the WHOLE look + content (brand, logo, favicons, colors,
  font sizes, nav, announcement, hero+images, tiles, pills, testimonials, legal, legalLinks).
  Change the store by editing THIS first. Colors/sizes → CSS vars injected in `app/root.jsx`.
- **`app/styles/app.css`** — all styles (`.tob*` classes). Large file — use edit_store_file (surgical).
- **`app/components/`** — Header.jsx (logo + hamburger + cart/search icons; nav is hamburger-only),
  Footer.jsx (Shop + Support + Legal links), PageLayout.jsx (marquee + header + main + footer + mobile drawer),
  ProductItem.jsx (product card), ProductForm.jsx (variant selectors + add-to-cart).
- **`app/routes/`** — `_index.jsx` (home: hero carousel, pills, tiles, product grid, testimonials),
  `products.$handle.jsx`, `collections.$handle.jsx`, `cart.jsx` (full page), `search.jsx`,
  `policies.$handle.jsx` (Privacy/Refund/Shipping/Terms pulled LIVE from Shopify),
  `pages.accessibility.jsx` + `pages.contact.jsx` (from `app/lib/legal.js`).
- **`public/images/`** — hero-1..5.jpg + tile-{baby-boys,baby-girls,unisex}.jpg (LOCAL, CSP-safe).
- **`store-profiles/timeforbaby/`** — `theme.config.json` + `store.env` (deploy token). Deploy = `./scripts/deploy.sh timeforbaby`.

## How it works
- Products + collections come LIVE from the Shopify Storefront API (nothing hardcoded).
- Policies come LIVE from Shopify. Checkout → Shopify (needs a payment provider enabled).
- Nav links live ONLY in the hamburger drawer (Header.jsx → PageLayout MobileMenuAside).
- ENGLISH-ONLY storefront. Production deploy = `./scripts/deploy.sh timeforbaby` (--env-branch main).

## Recent changes (append newest on top — one line each)
- 2026-07-07 — MOBILE header/marquee fixes per Itzik (branch mobile-header-fixes, deployed PREVIEW only). (1) Payment icons weren't visible on mobile in the black bar (scrolled off inside the marquee track) → added a STATIC pinned strip `.tob-ann-fixedpay` anchored right of `.tob-ann` (gradient fade so text scrolls behind it); on ≤820px the in-track `.tob-ann-pay` is hidden and the pinned strip shows. (2) Tighter mobile navbar: `.tob-hrow` padding 10px top/bottom, smaller logo text. (3) Header no longer permanently sticky — new `useScrollDirection()` hook in PageLayout toggles `.tob-chrome-up/down` on `.tob`; CSS translates `.tob-header` up (-100%) on scroll-down, back to 0 on scroll-up (marquee stays non-sticky at top so no overlap). Build passed. Preview: 01kwsp7813bsd35d6z0m2efmnc-5bb253f669a81db0c9b9.myshopify.dev
- 2026-07-07 — RELEASE: merged hydrogen-storefront → main (payment icons in marquee + search fix + product/variant audit data fixes), build passed, deployed to PRODUCTION via ./scripts/deploy.sh timeforbaby. Note: `git push origin main` is NOT allow-listed, so origin/main is 28 commits ahead locally but not pushed — deploy to Oxygen went straight from local main.
- 2026-07-07 — Product/variant audit per Itzik. Confirmed all 24 products: have a main image, have >2 variants (mostly 4-8 sizes, some 6-16 colors), and SIZE+COLOR buttons work together (ProductForm navigates to the exact variant per combo). ProductForm ALREADY hides an option button when it has only 1 value (`if optionValues.length===1 return null`) — so "no SIZE/COLOR → no button" is satisfied, no code change needed. Inventory is UNTRACKED (inventoryItem.tracked=false, policy DENY) → availableForSale=true always = correct for CJ dropship (never false "Sold out"). DATA FIX: deleted 5 junk variants (Color value literally "Color") from "Cute Cartoon Baby Bodysuit" (gid 7590363660359) → Color option now clean (15 real colors). NO code build/deploy (Shopify data only). Adding MORE products: CJ API returned empty for baby clothes/romper/bodysuit keywords this session (CJ-side rate/connection hiccup) — retry later to source+push new items.
- 2026-07-07 — FIX payment icons not showing in marquee: the prev session's config edit was NEVER saved — BOTH theme.config.json (app/ + store-profiles/timeforbaby/) had reverted to the OLD `announcement` (FREE SHIPPING…/RETURNS/ORGANIC/DESIGNED) with NO `paymentIcons` key, so PageLayout's `PAY_ICONS = config.paymentIcons || []` was empty → nothing rendered (SVGs + CSS `.tob-ann-pay` + PageLayout JSX were all present, just no DATA). Re-added to BOTH config files: `announcement` now includes "WE ACCEPT PAYPAL · VISA · MASTERCARD · ISRACARD & MORE" + "SECURE CHECKOUT", and `paymentIcons` array (paypal/visa/mastercard/isracard → /images/pay-*.svg). Build passed, deployed PREVIEW.
- 2026-07-07 — Marquee: added payment messaging + brand icons per Itzik. theme.config.json `announcement` now includes "WE ACCEPT PAYPAL · VISA · MASTERCARD · ISRACARD & MORE" + "SECURE CHECKOUT". Added new `paymentIcons` array (paypal/visa/mastercard/isracard → local SVGs in public/images/pay-*.svg, CSP-safe, self-made). PageLayout AnnouncementMarquee now renders a `.tob-ann-pay` group of `<img>` icons inside the scroll track (unit duplicated for seamless -50% loop). CSS `.tob-ann-pay`/`.tob-ann-pay-ico` (23px tall, subtle white ring). Only the 4 requested icons added. Build passed, deployed PREVIEW. NOTE: Hype gateway (סליקה) is a Shopify admin/payment-provider setup, not code — awaiting Itzik.
- 2026-07-07 — FIX search 500: the "browse all on focus" was submitting `q=*` to the predictive endpoint, but Shopify's predictiveSearch REJECTS an empty/`*` term → threw → /search loader's `.catch` didn't actually stop `return await searchPromise` → 500. Fixes: (1) SearchFormPredictive.fetchResults now returns early on empty input (no `*` request). (2) Header dropdown: when field is empty (`!term.current`) it shows a plain "Browse all products →" link to /collections/all — no network call. (3) /search loader wrapped in try/catch that returns a graceful empty result (type-aware) instead of ever 500-ing. Build passed, deployed PREVIEW.
- 2026-07-07 — Search "browse all on focus" per Itzik: HeaderSearch now fires fetchResults on focus/click; empty field submits q=`*` (limit 20) so the predictive endpoint returns the FULL catalog as a "All products" list BEFORE typing (blank term → isBrowseAll → shows Products only + "Browse all products →" → /collections/all). Removed the OLD /search magnifier button (tob-hsearch-mob Link) from Header; search FIELD is now always visible (added @media <820px: full-width field + full-width drop; flipped the old `.tob-hsearch-wrap{display:none}` mobile rule to flex). Added .tob-hsearch-head style. Build passed, deployed to PREVIEW. NOTE: 6 products have only 1 image (Bamboo Baby Loungewear Romper, Cotton Baby Essentials Set, Soft Waffle Baby Summer Set, Soft Bamboo Baby Romper, Baby Cotton Monk Onesie, Cozy Newborn One-Piece Romper) — flagged, NOT hidden yet; CJ API returned empty on all keywords so couldn't source 2nd images / new products this run.
- 2026-07-06 — Fixed header search per Itzik (2 bugs): (1) Navbar search NO LONGER jumps to /search — replaced the plain `<Form action="/search">` in Header.jsx (`HeaderSearch`) with a LIVE predictive dropdown: uses SearchFormPredictive + SearchResultsPredictive, results render in a `.tob-hsearch-drop` panel BELOW the field (Products/Collections/Pages + "View all results →" link to /search on Enter/click). Opens on focus/typing, closes on outside-click (useEffect doc listener) or result click. (2) Search now returns products — predictive endpoint queries the Storefront predictiveSearch directly (the /search page still exists as full-results fallback). Added full `.tob-hsearch-wrap`/`.tob-hsearch-drop` CSS (positioned dropdown, thumbs, hover, "View all" footer); mobile <820px keeps the /search icon, desktop shows the live bar. Build passed.
- 2026-07-05 — 5 fixes per Itzik: (1) Product-page buttons now ONE language (app.css): Add-to-cart got `tob-atc-btn` (solid dark primary) + Buy-it-now `tob-buynow-btn` outline — same 54px height/radius/font so they match the rest of the store (was mismatched). (2) Cart "Continue to Checkout" CTA resized to 52px, no longer oversized. (3) Discount-code ↔ gift-card gap killed: sections + inputs/buttons now compact single rows (input flex + 42px Apply button; stray br hidden). (4) Cart "Continue shopping" now → `/` (home) instead of the ugly `/collections` page (CartMain.jsx). (5) Search moved to the header ALWAYS: added pill search field `tob-hsearch` (GET /search) in Header.jsx, shown ≥820px; mobile keeps the /search icon (`tob-hsearch-mob`), bar hidden <820px. Also fixed "price doesn't update fast" in cart: added OPTIMISTIC line total (CartLineItem: perUnit×qty) + optimistic subtotal (CartMain recomputes cost.subtotalAmount from lines) so amounts move instantly on +/− instead of after a server round-trip. Build passed, deployed.
- 2026-07-05 — Fixed cart DRAWER desktop layout (app.css): the ≥820px `@media (min-width:820px)` block (cart PAGE 2-col grid) was leaking onto the drawer's `.cart-details`, putting line items + summary side-by-side/overlapping with Checkout cut off. Added drawer-scoped overrides INSIDE that media query (`.tob .overlay[data-type="cart"] aside{width:400px;max-width:92vw}` + force `.cart-details` back to single-column flex, summary full-width). Also added a base `.cart-summary-aside` block: stacked summary, Subtotal as space-between row, discount/gift inputs on one line, full-width 52px dark Continue-to-Checkout CTA (hover/focus ring), `br` hidden. Matches mobile. Build passed, deployed.
- 2026-07-05 — Added express "Buy it now" button on product page (ProductForm.jsx) under Add-to-cart: uses AddToCartButton with redirectTo="checkout" sentinel; cart.jsx action converts "checkout" → cartResult.checkoutUrl (303) so shopper lands straight on Shopify checkout (PayPal/dynamic buttons show there once enabled in Settings→Payments). AddToCartButton now accepts className. Styled .tob-buynow (outline→fill hover, 52px min, focus ring) per ui-ux-pro. Build passed, deployed.
- 2026-07-05 — Cart drawer CSS fix (app.css): added missing `.sr-only` util (hides "Line items" label); added full `.tob .cart-*` block (Subtotal now a flex space-between row so the AMOUNT shows on the right; line items with 76px thumbs, qty/remove controls, primary checkout CTA, drawer flex-column so summary pins to bottom; cart PAGE gets 2-col grid ≥820px). Build passed, deployed.
- 2026-07-05 — Fixed collection categorization: moved 3 from Baby Boys (Fishing Print, Classic Crew Neck, Cozy Autumn Layered — all described as "unisex") + 2 from Baby Girls (Cotton Baby Essentials Set, Cozy Split-Leg Sleepsuit — gender-neutral prints) into Unisex via collectionAdd/RemoveProducts.
- 2026-07-05 — nav moved to hamburger-only (Header.jsx, PageLayout.jsx, app.css); local hero/tile images.
- 2026-07-05 — Desktop nav restored (DesktopNav in Header.jsx, CSS ≥820px); hamburger hidden on desktop, shown on mobile; product image CSS fixed (.tob .product-image + .tobp grid); deployed.
- 2026-07-05 — Bug fix: added productPage.trustBadges to theme.config.json (was missing → runtime crash on product page); fixed hero secondaryLink /pages/about-us → /collections/all (was 404); deployed.
