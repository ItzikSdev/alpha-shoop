# Agent Status Matrix

Snapshot from the 2026-08-20 agent-reliability audit/fix session. Overwrite this
file next time agents are re-verified — it is a point-in-time snapshot, not a
changelog (see `docs/DECISIONS_LOG.md` for the dated history of what was found
and fixed).

| Agent | Real job (charter) | Dispatch path tested today? | Known issues found | Fix status | Still unverified |
|---|---|---|---|---|---|
| **Sol** | CJ sourcing, copy, Shopify push; own LLM tool-loop (`agent_loop.py`) | Yes — ticket-tick, order-poll fallback investigation, `_TOOLS` cross-check | Timeout handling gaps, gid/PID resolution issues, duplicate-ask noise | Timeout/gid/ask-cooldown fixes applied and verified live | Nothing new from today; broader charter-drift monitoring is Nova's job now |
| **Reel** | Product video/image media (Veo rotation + Gemini images) | Yes — live dispatch, classifier fix, Wan2.2 removal | Classifier truncation → false "started" narration with no real render; AVATAR_UGC/PRODUCT_3D_SHOWCASE routed to deleted Wan2.2 pipeline | Fixed and verified live today (retry+`/no_think` classifier fix; Wan2.2 path disabled, charter/tool_catalog updated) | — |
| **Milo** | Order fulfillment: CJ order → tracking → customer email | Yes — full pipeline, real test orders, cleaned up after | No webhook registered (no public URL); `fulfill_shopify_order` used deprecated REST (406); `process_order` silently discarded email/tracking failures as "✅ shipped"; `tool_catalog.py` had wrong tools listed | Fixed and verified live today: poll-based trigger, FulfillmentOrder API, verify-then-fallback email (Shopify `Order.events` check → Nora/Resend fallback, both logged to the knowledge graph), `tool_catalog.py` corrected | Real webhook registration still blocked on this backend having no public HTTPS URL — deferred, not fixed |
| **Ava** (CEO) | Orchestration, Shopify admin, routing | Not dispatched live today | None found today | N/A | `dispatch_to_agent` has a real branch (`_SHOPIFY_DOERS` includes "CEO" → `_agent_act_shopify`) — branch existence confirmed by reading the code, not live-tested today |
| **Kai** | TikTok ads reporting (read-only) | Not dispatched live today | None found today | N/A | `dispatch_to_agent` has a real branch (`"Growth Marketing Analyst"` → `_agent_act_ads`, no LLM call, real TikTok MCP calls) — branch existence confirmed by reading the code, not live-tested today |
| **Nora** | Customer support email | Heartbeat "do" path tested live today; `_support_inbox_loop` already known-working (separate mechanism, not retested) | Heartbeat/dispatch "do" path has no real branch — falls to generic `_agent_reply`. Live-tested: asked her to check for urgent complaints, she replied by paraphrasing her own charter, no `check_inbox` call, no real action | Not fixed — explicitly out of scope per earlier instruction ("don't touch Milo/Nora's missing handlers yet") | Whether/how to wire a real `_agent_act_nora` handler — open decision, not made unilaterally |
| **Nova** | Weekly oversight, graph gap-detection, docs-grounded chat | Yes — manually triggered real ticks today | Found live: her tick output a bare `{"type":"create_ticket",...}` instead of the menu's `{"type":"do","task":"create_ticket: ..."}` shape → `execute_decisions` logged "unknown decision type" and no ticket ever opened despite her narrating that it did. Root-caused to two separate bugs: (1) no normalization for a bare `create_ticket`/`close_ticket` type, (2) even the correctly-shaped `"do"` task wasn't reliably classified as a ticket op by `_agent_act_ops`'s natural-language-tuned prompt | **Fixed and verified today**: (1) `heartbeat.py` now normalizes a bare `create_ticket`/`close_ticket` type into the real `"do"` dispatch instead of dropping it; (2) `_OPS_SYS` (`conversation.py`) now explicitly recognizes a `create_ticket:`/`close_ticket:`-prefixed task as the ticket op regardless of phrasing. Verified via a direct real-path call (`dispatch_to_agent` → real classifier → `open_ticket`): a real ticket was created (deleted after, test data) | **qwen3 non-determinism**: a repeat real call with the identical fixed code returned `op:"none"` instead of `op:"ticket"` on one attempt even at temperature 0 — the fix raises the success rate but does not guarantee every single turn opens a ticket. A separate real tick afterward correctly chose `flag_blocker` (a different, valid decision) rather than re-exercising the create_ticket path, so full end-to-end reliability across many consecutive real ticks is not yet established |
