# Session handoff — 2026-08-13 (branch `inline-product-expand`)

## Running right now
- Docker: `redis`, `litellm`, `falkordb` up. Docker Desktop was fully down at session start.
- API: host uvicorn on :8000, started with `TRACES_DB_PATH="$PWD/data/traces.db"` (mandatory).
- Heartbeat is live again — agents are taking turns.

## Shipped this session (ALL UNCOMMITTED)
| file | change |
|---|---|
| `src/mcp_tools/shopify.py` | publish gate moved into `_publish_product` (≥3 images, real 360° video, live CJ stock, fails closed); `PublishBlocked`; supplier video tagged `supplier::`; `create_shopify_product` no longer auto-publishes |
| `src/org/agent_loop.py` | `ask_teammate`, `cj_stock_sweep`, `_team_block()` live roster, gate paragraph rewritten name-free |
| `src/org/conversation.py` | `dispatch_to_agent(..., return_reply=True)`; `company_language()` reads Settings |
| `src/org/heartbeat.py` | JSON-schema `response_format` on agent turns; timeout 90s → 240s |
| `src/config.py` | `org_language` Settings field (default English) |
| `src/org/seed.py` | Hebrew → English in Ava's + Sol's charters (DB reseeded, verified clean) |
| `src/org/tool_catalog.py` | `teamwork: ask_teammate`, `cj_stock_sweep` |
| `.claude/skills/shop-context/SKILL.md` | §0b NO HARDCODING — the LLM decides |

## Verified working
- Sol run `809bdedf`: dispatched via `POST /api/v1/org/sol`, chose `cj_stock_sweep(remove=False)` himself, 32/32 in stock, deleted nothing, 3m54s.
- Agent turns now parse (schema) and speak English (Milo, 61s).

## Open threads
1. **Nothing is out of stock** — CJ says 32/32 in stock; Shopify shows 0 unavailable variants, 0 tracked variants. If Itzik still sees "sold out", it's the Hydrogen UI — needs the specific product.
2. **12 live products have no 360° video.** The gate only guards NEW publishes. Unpublishing them was offered, never approved.
3. **Video pipeline dead since 2026-08-09** — Gemini `429 RESOURCE_EXHAUSTED` mid-batch (28 failures that day, 64 total). One video stuck in `awaiting_cost_approval` since 08-09.
4. **Sol's sourcing runs still time out** — transcript balloons with full CJ product JSONs; cap tool-result size. Not done.
5. `cj_search_products` timing out (~6 min) — the blocker the agents keep flagging.
