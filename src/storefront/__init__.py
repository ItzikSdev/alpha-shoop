"""Host-side Hydrogen storefront runner package.

This package runs OUTSIDE Docker, directly on the host, because it manages
local Node/Hydrogen dev servers and the Shopify CLI under
``<repo>/stores/shopify/{slug}/``.
"""
